@model List<ProgrammListingViewModel>
@{
    ViewData["Title"] = "All Programms";
}

<div class=" table">
    <h2 class="heading-margin text-center">All Programms</h2>
    <table class="table table-striped">
        <thead>
            <tr>
                <th scope="col">Programm Name</th>
                <th scope="col">Url</th>
                <th scope="col">Action</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var programm in Model)
            {
                <tr>
                    <td>@programm.ProgrammName</td>
                    <td>
                        <a href=@programm.Url>
                            <div style="height:100%;width:100%">
                                @programm.Url
                            </div>
                        </a>
                    </td>

                    <td>
                        <a asp-controller="Customers" asp-action="Edit" class="btn btn-primary btn-sm">Edit</a>
                        <button id="buttonDelete" type="button" class="btn btn-primary btn-sm">Delete</button>
                    </td>
                </tr>
            }
        </tbody>
    </table>
    <a asp-controller="Programms" asp-action="Add" class="btn btn-primary btn-sm">Add new Programm</a>
</div>
